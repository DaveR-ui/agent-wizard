{
  "prompt": "we need a way for admins to export the permission table as CSV",
  "packet": {
    "normalized_goal": "Design CSV export for the permission table",
    "type": "feature-design",
    "confidence": "medium",
    "modules": ["permissions"],
    "constraints": ["Export must respect row-level permissions"],
    "non_goals": ["Bulk import"],
    "hidden_assumption": "This plan assumes a backend endpoint will do the export. If export must be client-side, the design changes.",
    "acceptance_criteria": ["Admins can download a CSV of the permission table"],
    "edge_cases": ["Empty permission table", "Large exports"],
    "unresolved_questions": [
      {
        "question": "CSV or XLSX?",
        "could_not_resolve": "no slice keyword maps 'export' to a single format"
      }
    ],
    "clarification_needed": "yes",
    "blocking_questions": ["CSV or XLSX?"]
  }
}
